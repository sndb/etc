etc -- dotfiles
===============

- to install:

        stow -vt ~ bash neovim tmux ...

- to restow:

        stow -Rvt ~ bash neovim tmux ...

- to delete:

        stow -Dvt ~ bash neovim tmux ...
