#!/bin/sh -eu
# set a wallpaper

dir="$XDG_DATA_HOME/wallpapers/"

if [ -h "$dir/default" ]; then
	wp="$dir/default"
	echo "wallpaper: default"
else
	wp="$(find "$dir" -type f | shuf -n 1)"
	echo "wallpaper: random ($wp)"
fi

hsetroot -cover "$wp"
