#!/bin/sh

# disable dpms and prevent screen from blanking
xset s off -dpms &

# activate numlock
numlockx &

# notifications
dunst &

# statusbar script
bar &

# wallpaper
xsetroot -fg "#221000" -bg "#442c2f" -mod 4 4 &

# load resources
xrdb -merge ~/.Xresources

exec dwm
