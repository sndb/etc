#!/bin/sh

# prevent screen from blanking
xset s off -dpms

# activate numlock
numlockx

# set default cursor
xsetroot -cursor_name left_ptr

# load resource file
xrdb -merge ~/.Xresources

# enable hotkey daemon
sxhkd &

exec bspwm
