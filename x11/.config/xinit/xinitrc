#!/bin/sh

# prevent screen from blanking
xset s off -dpms

# activate numlock
numlockx

# load resource file
xrdb -merge ~/.Xresources

# set background
xsetroot -solid "#442c2f"

# status bar
bar &

exec dwm
